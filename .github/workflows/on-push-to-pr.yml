name: On Push to PR

on:
  pull_request:
    types: [synchronize]

jobs:
  remove-label:
    runs-on: ubuntu-latest
    steps:
      - uses: bmsiegel/action-remove-labels@a521ee2643bf12c66df7b8dd38a567f0d8e2209c
        with:
          labels:
            'safe to test'
          #We want to make this false because if the label
          #isn't there, we don't want to fail. Worst case
          #the label stays and we remove/re-add.
          fail_on_error: 'false'
