Use whitespace consistent with the pre-existing whitespace. For instance, if the rest of the file already uses tab 
characters (\t), then indent with tab characters. If the file uses 4 spaces per indent, then use 4 spaces.

DO NOT replace preexisting tabs with spaces