To run the formatter, run the `make fmt` command in the root directory

To run unit tests, run the `make test` command in the root directory

All commits must first be formatted and pass all unit tests.
